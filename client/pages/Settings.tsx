import Layout from "../components/layout/Layout";

export default function Settings() {
  return (
    <Layout>
      <div className="rounded-lg border border-border/60 bg-card/60 p-8 text-center text-muted-foreground">
        Settings page placeholder. Ask to add preferences like theme, alerts, and wallet options.
      </div>
    </Layout>
  );
}
